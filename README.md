# Minimaps

ESP32 firmware lives in `Minimaps.ino`.

A new Flutter mobile client was added in `mobile_app/` to fetch Vietmap routes, project the next 10 route points into TFT coordinates, stream BLE payloads to the ESP32, and expose the pocket-mode screen lock UI.
